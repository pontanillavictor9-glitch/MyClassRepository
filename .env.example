# Copia este archivo como  .env  (sin el .example) y rellena los valores.
# El archivo .env está en .gitignore: nunca se sube a GitHub.

# API key de ElevenLabs para ESTE proyecto (canal de YouTube).
# Se crea en https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=sk_pega_aqui_tu_clave
